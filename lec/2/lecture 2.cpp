#include <iostream>
#include <cctype>
#include <cstdlib>
#include <fstream>

using namespace std;

int main(/*int argc, char *argv[]*/)
{
	/*if (argc==2){ //checks to see that you pass in an argument OTHER than run argument
		ifstream file;
		file.open(argv[1]); //arg 1, arg 0 is ./name
		string aString; //empty string
		getline(file,aString); //stores into aString
	
		cout<<aString<<endl;
	
		file.close(); //always close
	}
	else{
		cout<<"ERROR: No Argument"<<endl;
	}
	return 0;*/


	//checks for number
	/*
	int v = 3;
	int A[5];
	for (int i=0; i<A.len; i++){ //create array
		A[i] = i;
		cout<<A[i]<<endl;
	}
	for (int i=0; i<A.len; i++){ //look through array
		if (A[i]==v){ //look for v
			for (int j=i; j<A.len-1; j++)
				A[j]=A[j+1]; //replace and shift
			i=i-1; //moves back, so it checks the space again, in case v appears twice in a row

		}
	}

	for (int i=0; i<5; i++){ //create array
		cout<<A[i]<<endl;
	}
	*/


}